import 'package:flutter/material.dart';

class SpecialPlan extends StatelessWidget {
  const SpecialPlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
